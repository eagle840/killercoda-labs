# Step 2: Configure OmniRouter

Install and configure the OmniRouter AI gateway.

### 1. Install OmniRouter
`npm install -g omniroute`{{exec}}

### 2. Run OmniRouter
`host=0.0.0.0 omniroute`{{exec}}

### 3. Verify Traffic
OmniRouter is now running on port 20128.
{{TRAFFIC_HOST1_20128}}
